apiVersion: v1
kind: Secret
metadata:
  name: devflow-postgresql
  labels:
    app.kubernetes.io/name: devflow-postgresql
    app.kubernetes.io/part-of: devflow
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ required "secrets.postgresPassword is required" .Values.secrets.postgresPassword | quote }}
