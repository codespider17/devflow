apiVersion: v1
kind: Service
metadata:
  name: devflow-api
  labels:
    app.kubernetes.io/name: devflow-api
    app.kubernetes.io/part-of: devflow
spec:
  selector:
    app.kubernetes.io/name: devflow-api
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
